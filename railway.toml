[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "echo 'This is a monorepo. Deploy individual services using their specific Dockerfiles.'"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
