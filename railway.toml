[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm build"

[deploy]
startCommand = "echo 'This is a monorepo. Deploy individual services.'"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
